import React from 'react';
import { View } from 'react-native';
import { Typography } from '@/components/Typography';

const LoginScreen = () => {
  return (
    <View>
      <Typography label='LoginScreen' variant="bodySmall" />
    </View>
  );
};

export default LoginScreen;
