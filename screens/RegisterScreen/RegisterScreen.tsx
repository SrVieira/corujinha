import React from 'react';
import { View } from 'react-native';
import { Typography } from '@/components/Typography';

const RegisterScreen = () => {
  return (
    <View>
      <Typography label='RegisterScreen' variant="bodySmall" />
    </View>
  );
};

export default RegisterScreen;
